Django==4.0
python-decouple==3.8
gunicorn==22.0.0
# Juez IA de la suite de auto-testing (landing/autotesting.py). Opcional:
# sin OPENAI_API_KEY la suite evalúa con reglas y no necesita este paquete.
openai==2.44.0
# Solo si usas PostgreSQL/Supabase como base de datos en producción:
psycopg2-binary==2.9.9
